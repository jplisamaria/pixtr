 <%= div_for comment do %>
    <%= comment.body %> <em><small> comment by: 
    <%= comment.user.email %>,
    <%= time_ago_in_words(comment.created_at) %> ago </small></em>
    <%= link_to "Delete Comment", image_comment_path(comment.image, comment), method: :delete, remote: true %>
<% end %>